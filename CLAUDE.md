# SnackShot — 동영상 다이어리

본인 사용 도구. AI 어시스턴트와의 페어 코딩으로 개발 중.
모든 의사결정은 `SnackShot-ADR.md`와 `SnackShot-ADR-Phase0.md`에 기록되어 있다.
문서 권위 등급(어떤 문서를 규칙으로 따를지)은 `docs/INDEX.md`를 따른다.
UI/UX는 `SnackShot-DesignSystem.md`(디자인 시스템)와 `src/theme/`(디자인 토큰)을 기준으로 한다.

## 기술 스택
- React Native 0.83 + Expo SDK 55 (Dev Client, New Architecture)
- TypeScript strict mode
- expo-router (file-based routing, Next.js app router 유사)
- Zustand (상태관리)
- expo-sqlite (openDatabaseAsync API만 사용, legacy API 금지)
- ULID (ID 생성)
- Zod (스키마 검증)
- date-fns (시간)

**⚠️ Expo SDK 55는 학습 데이터보다 최신이다.** 네이티브 모듈 API 사용 전 반드시 버전 문서(https://docs.expo.dev/versions/v55.0.0/)를 확인하라. 예: expo-file-system은 File/Directory 클래스 기반 동기 API로 전면 교체되어 구 `*Async` API(makeDirectoryAsync 등)는 런타임에서 throw한다.

## 설계 원칙 (ADR 요약)
1. **클립이 1급 객체** (ADR-003). Day는 시간순 그룹화 뷰.
2. **시각은 UTC Unix ms (INTEGER)** (ADR-013). UI 표시 시에만 로컬 변환.
3. **Soft delete** (ADR-014). 모든 쿼리에 `WHERE deleted_at IS NULL`.
4. **ID는 ULID** (ADR-009). `@/lib/id.ts`의 `newId()` 사용.
5. **DB 컬럼 snake_case ↔ TS 객체 camelCase**. 변환은 repo 안에서 (ADR 본문).
6. **AI 원본 보존** (ADR-016). 사용자 편집본은 별도 컬럼.
7. **Transcript는 별도 테이블** (ADR-010). 1:N.
8. **백그라운드 큐는 DB 기반** (ADR-012). ai_jobs 테이블.
9. **STT/AI 라벨링은 인터페이스로 추상화** (ADR-002, ADR-008). 구현체 교체 자유.

## 폴더 컨벤션
- `app/` — expo-router 화면 (직접 수정)
- `src/db/` — schema, migrations, repos (snake_case SQL은 여기만), `mapping.ts`(row→도메인 매퍼 빌더 `makeRowMapper`)
- `src/services/` — STT, label, video, jobs (인터페이스 + 구현체 분리)
- `src/stores/` — zustand stores
- `src/components/` — 재사용 UI (화면별 하위 폴더 가능, 예: `archive/`)
- `src/lib/` — id, time 등 유틸
- `src/types/` — 도메인 타입 (camelCase)
- `src/theme/` — 디자인 토큰(색·타이포·간격·그림자·모션). 원시 palette는 내부 전용
- `@/` alias → `src/`

## 코딩 스타일
- async/await (Promise.then 금지)
- 함수형 repo (클래스/DI 금지)
- 명시적 타입 (`any` 금지, `unknown`은 OK)
- 한 파일 200줄 이내 권장
- import는 `@/...` 우선
- 한 번에 한 파일씩 단순하게
- 색·간격·라운드·그림자·폰트는 `@/theme` 토큰 사용. 텍스트는 `theme.text.*` 프리셋 경유

## 작업 흐름
1. 변경 후 `npm run verify` 통과 확인 (= `tsc --noEmit` + 불변식 `scripts/check-invariants.sh` + 마이그레이션 append-only `scripts/check-migrations.mjs`; CI `verify.yml`와 동일)
2. DB 관련 변경은 콘솔 로그로 동작 확인
3. UI 변경은 에뮬레이터 시각 확인 (Metro reload)
4. ADR 위반 시 즉시 지적

## 절대 금지
- ADR 결정 임의 변경 (질문 후 ADR 갱신)
- `expo-sqlite` legacy callback API 사용
- `any` 타입
- snake_case 변수명 (DB row 매핑 외)
- expo-av 사용 (deprecated, expo-video/expo-audio 사용)
- 색상값(`#RRGGBB`)·스타일 매직넘버 하드코딩 (토큰 사용. 신규 값 필요 시 토큰에 추가 후 참조)
- `src/theme/tokens.ts`의 `palette` 직접 import (semantic 토큰 경유)

## 하네스: SnackShot 개발

**목표:** DB/Service/UI 레이어별 전문 에이전트가 협력하여 ADR 원칙을 준수한 기능을 구현한다.

**트리거:** 코드를 직접 변경하는 개발 요청 시 `feature-dev` 스킬을 사용하라. 단순 질문·설명·ADR 검토는 직접 응답 가능.

**변경 이력:**
| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-06-10 | 초기 구성 | 전체 | - |
| 2026-06-11 | ADR 파일명 오타 수정 (ShackShot→SnackShot) | SnackShot-ADR.md | planner가 메인 ADR을 읽지 못하던 문제 |
| 2026-06-11 | grep 오탐 수정(멀티라인 SQL, withTransactionAsync), 허용 예외 명시, 파일 크기 경고 추가 | agents/qa-engineer.md | 첫 QA 실행이 가짜 FAIL을 내는 문제 |
| 2026-06-11 | 서비스 구조도 현행화 + "실제 파일 우선" 원칙 | agents/service-engineer.md | 문서-코드 drift (ISttService/JobRunner 등 실존하지 않는 구조) |
| 2026-06-11 | 라우트 맵 현행화 + store 데이터 접근 규칙 현실화 | agents/ui-engineer.md | 문서-코드 drift, 사문화된 규칙 |
| 2026-06-11 | ADR-011 오인용 수정 | agents/db-engineer.md | 잘못된 근거 인용 |
| 2026-06-11 | 구현 에이전트에 원요청 요약 전달, 완료 보고에 하네스 개선점 항목 추가 | skills/feature-dev | 뉘앙스 손실 방지, 피드백 루프 가동 |
| 2026-06-11 | Expo SDK 55 문서 확인 지침 흡수 (AGENTS.md 고아화 해소), _workspace gitignore | CLAUDE.md, .gitignore | 지침이 어떤 에이전트에게도 전달되지 않던 문제 |
| 2026-06-15 | 디자인 시스템/토큰 신설(`src/theme/`, `SnackShot-DesignSystem.md`), 토큰 사용 규칙·하드코딩 금지 추가 | CLAUDE.md, src/theme, agents/ui-engineer.md | UI/UX 개편 기반 마련, 화면별 색 하드코딩 제거 |
| 2026-06-16 | P0 리팩토링: 죽은 stub(WhisperSttService) 제거, AGENTS.md→포인터, 탐색문서 `docs/explorations/` 격리 + `docs/INDEX.md` 권위표 신설, 에이전트 문서 구조 스냅샷→코드 진실원 전환·도메인 드리프트 수정, `_workspace_prev*` 제거 | 전체 하네스·docs | 문서-코드 드리프트 차단, AI 컨텍스트 비용 절감 |
| 2026-06-16 | P1 리팩토링: `INVARIANTS.md`(기계가독 규칙표, qa grep과 1:1) 신설, 도메인 enum 단일 진실원 `src/types/enums.ts`로 통합(domain.ts·label/schema.ts 재사용), schema.ts 반복 FTS 트리거·인덱스 SQL 상수 추출(컴파일 MIGRATIONS 바이트 동일 검증, 621→512줄) | INVARIANTS.md, src/types/enums.ts, src/db/schema.ts, 하네스 | enum 다중 동기화 제거, 규칙 로드 비용↓ |
| 2026-06-16 | P2-2: 불변식 자동 게이트 신설 — `scripts/check-invariants.sh`(오탐0 부분집합 하드 강제)·`scripts/check-migrations.mjs`(마이그레이션 append-only 해시 락 `migrations.lock.json`), `npm run verify` + CI `verify.yml`(tsc+불변식+마이그레이션)로 연결 | scripts/, .github/workflows/verify.yml, package.json, INVARIANTS.md, qa-engineer.md | 규칙을 사후 grep→작성시점 강제로 이동, 회귀 차단 |
| 2026-06-16 | P2-1: repo row↔도메인 매핑 표준화 — `src/db/mapping.ts`의 `makeRowMapper<T>` 도입, entries/transcripts/decisions/outcomes/aiJobs의 손수 `to*()` 매퍼·Row 인터페이스를 카탈로그 기반으로 전환(도메인 필드 누락을 컴파일 타임 강제), 검색 Entry 매퍼 중복 제거(toEntry 재사용) | src/db/mapping.ts, src/db/repos/*, db-engineer.md | 컬럼 추가 시 수작업 동기화 지점 축소, 누락 매핑 버그 차단 |
| 2026-06-16 | P3 리팩토링: archive 화면의 캘린더 표현 컴포넌트(CalendarDay/WeekStrip/PhotoStack)를 `src/components/archive/CalendarParts.tsx`로 분리(680→531줄), `docs/CODEMAP.md`(엔티티/화면 역색인) 신설, 주요 화면에 `@codemap` 헤더 추가 | app/(tabs)/*, app/entry, src/components/archive, docs/CODEMAP.md | 대형 파일 컨텍스트 비용↓, AI 탐색성↑ |
| 2026-06-16 | 검토 후속: P2-1에서 도입했으나 미사용으로 남은 헬퍼(`sqlCheck`/`makeGuard`) 제거(자기 도그푸딩 — dead code 금지), `makeRowMapper`에 `__DEV__` 'req' 컬럼 누락 경고 추가(컬럼명 오타 조기 발견) | src/types/enums.ts, src/types/domain.ts, src/db/mapping.ts, INVARIANTS.md | 미사용 표면 축소, 매퍼 컬럼명 안전 보완 |
| 2026-06-16 | A: preview(영상)·preview-audio(오디오)의 중복 저장 파이프라인(파일 이동→insertEntry→상태/메모→outcome/export→잡 큐잉)을 `src/services/saveCapturedEntry.ts` 단일 경로로 추출, 화면은 입력수집·UI 전환만 담당 | src/services/saveCapturedEntry.ts, app/preview*.tsx, docs/CODEMAP.md | 저장 로직 SoT화, 분기(mode) 일원화 |
| 2026-06-16 | B: `services/jobs/handlers.ts`(304줄)를 잡 타입별 파일로 분리 — `handlers/{compression,stt,obsidianExport,labelExtraction}.ts` + 제어흐름 에러 `signals.ts` + `index.ts` 배럴. 함수 본문 바이트 동일, queue.ts import 무변경(`./handlers`→디렉토리) | src/services/jobs/handlers/*, docs/CODEMAP.md | 핸들러 탐색성↑, 타입별 격리 |
| 2026-06-17 | 버그 수정: record.tsx의 `recordAsync`를 try/catch/finally로 감싸 네이티브 녹화 실패 시 uncaught rejection·UI 멈춤 방지(에뮬레이터 가상카메라 미지원 등) | app/record.tsx | 크래시→graceful 안내 |
| 2026-06-17 | 대형 화면 분할(P3-1 후속): settings/entry/today의 프레젠테이션 블록을 `src/components/{settings,entry,today}/`로 추출 — settings(533→348: KeyInputRow·ObsidianSyncSection), entry(416→338: FailureCard·EntryTextSection), today(408→300: TodayHeader·TodayComposer·ScrollFab). 화면은 상태·핸들러만, 컴포넌트는 props 기반 순수 렌더 | app/(tabs)/settings·today, app/entry, src/components/{settings,entry,today} | 화면 파일 컨텍스트 비용↓, 재사용 가능 | 
| 2026-06-17 | archive 검색 UI 분리: 검색바·최근검색·결과상태 블록을 `src/components/archive/ArchiveSearchBar.tsx`로 추출(535→472), store 접근은 부모가 props로 주입 | app/(tabs)/archive, src/components/archive | 검색 UI 격리, 화면 본문 축소 |
| 2026-06-20 | ADR-005 개정(Revision): 녹화 일시정지/이어찍기 도입 + 3분 상한을 누적 녹화시간 기준으로 재정의(영상은 `toggleRecordingAsyncAvailable` 게이팅). 초안 `docs/explorations/ADR-005-revision-draft.md` 승인 후 반영 | SnackShot-ADR.md, docs/INDEX.md, app/record*.tsx | 한 호흡 녹화 제약 해소 |
| 2026-07-03 | `.gitattributes` 신설(`* text=auto`, `*.sh`/`gradlew`는 `eol=lf` 강제) + 워킹트리 CRLF 스크립트 LF 복구 | .gitattributes, scripts/check-invariants.sh, android/gradlew | 워킹트리 전체 CRLF 변환으로 250개 파일 phantom-modified + 로컬 `npm run verify`가 bash CRLF 오류로 깨지던 문제 |
| 2026-07-03 | check-invariants.sh 강건화: grep exit≥2(실행 오류)를 하드 실패(exit 2)로 처리하는 `g()` 래퍼 도입, 저장소 루트 사전 가드 추가, `set -euo pipefail` | scripts/check-invariants.sh | grep이 잘못된 경로/패턴으로 죽어도 빈 결과=✓ 통과로 처리되던 조용한 오탐 차단 |
| 2026-07-03 | CODEMAP/INDEX 현행화: 6/16 이후 신규 표면 등재(TextRevision 엔티티, video/widget 서비스, original_backup 잡, storage·storage-files·compose-decision·decisions 라우트), INDEX enum 진실원 domain.ts→enums.ts 정정, 탐색문서 6건 상태 등재·정정 | docs/CODEMAP.md, docs/INDEX.md | 문서-코드 드리프트 해소(영상관리·위젯·결정작성 기능이 역색인에 없던 문제) |
| 2026-07-03 | v14 마이그레이션: `fts_entries_insert_note` 트리거(AFTER INSERT WHEN manual_note IS NOT NULL) 추가, insertTextEntry의 동일 값 더미 UPDATE 우회 제거. 인메모리 SQLite로 v1→v14 전체 실행 + 트리거 6케이스 동작 검증 | src/db/schema.ts, src/db/repos/entries.ts, scripts/migrations.lock.json | 텍스트 entry FTS 인덱싱이 repo의 더미 UPDATE 주석에 의존하던 함정 제거(쓰기 2회→1회, 스키마가 정합성 보장) |
| 2026-07-03 | 의사결정 강화 구현 지시서(D1~D4) 신설: 결정 FTS(v15)·회고 대조+learnings·export 확장+알림·대시보드/decision_links. 설계 확정치·함정 목록·수용 기준·인메모리 검증 레시피 포함. 기능 평가 결론: 저장 형태는 충분, 회수(검색·대조·연결·집계) 경로가 부재 | docs/explorations/SnackShot-Decision-Enhancement-Guide.md, docs/INDEX.md | 후속 구현 세션(Opus급)이 재설계 없이 기계적으로 실행 가능하도록 결정 고정 |
| 2026-07-03 | 사용 피드백 구현 지시서(E1~E3) 신설: E1 옵시디언 수신함 import(Inbox.md 단방향 채널, obsidian_import 잡, 해시 중복 방어), E2 추출 정밀도(중대성 기준·EX2 교체·confidence 접힘·반려 이력 few-shot 주입·hint 활용), E3 개인화(Profile.md + 최근 결정 다이제스트, 자동 메모리는 보류 결정). 권장 순서 E2→E1→E3, D 트랙과 마이그레이션 버전 조율 규칙 명시 | docs/explorations/SnackShot-UserFeedback-Guide.md, docs/INDEX.md | 과추출 원인(EX2 few-shot·무필터)을 코드에서 확정 진단, 양방향 sync 대신 수신함 설계로 위험 축소 |
| 2026-07-03 | D1 구현: 결정 전문검색 v15 `decisions_fts`(요약/상황/이유 user 우선 + 커스텀 카테고리 색인, insert/update/soft_delete 트리거 3개, rejected 포함 backfill) + `buildFtsQuery`를 `src/db/fts.ts`로 추출(transcripts/decisions 공용) + `searchDecisions` repo + 아카이브 '의사결정' 검색 칩(병행 검색·TimelineDecisionItem 렌더). 인메모리 v1→v15 수용기준 5개 통과, verify PASS | src/db/schema.ts, src/db/fts.ts, src/db/repos/{decisions,transcripts}.ts, src/stores/archive.ts, src/components/archive/SearchFilterChips.tsx, app/(tabs)/archive.tsx | 결정 회수 경로(검색) 신설 — Decision-Enhancement-Guide D1 |
| 2026-07-03 | D2 구현: 회고 대조 + learnings 수집(스키마 변경 없음, `outcomes.learnings` 재사용). OutcomeEditor에 `decision` 기반 '당시 기대'(expectedOutcome+확신도%, 없으면 생략) 블록·'다음에 적용할 교훈' 입력 추가, `onSubmit(result, reflection?, learnings?)` 확장 → inbox 화면 handleOutcome → store recordOutcome → insertOutcome까지 learnings 관통(tsc 강제). DecisionList 상세에 '교훈' 표시. verify PASS | src/components/OutcomeEditor.tsx, app/(tabs)/inbox.tsx, src/stores/inbox.ts, src/components/decision/DecisionList.tsx | 회고 회수(기대 대조·교훈 축적) 경로 신설 — Decision-Enhancement-Guide D2 |
| 2026-07-03 | D3 구현: (a) 옵시디언 export 확장 — buildDecisionCallout에 상황/이유(user 우선)·기대·확신도%·교훈 추가 + 제목 다음 줄 Dataview 인라인 필드(`[category::][confidence::][result::]`), 실제 함수 3케이스 콘솔 스냅샷 통과. 재export dirty는 기존 maybeEnqueueReExport(전일 재생성)로 이미 커버되어 clearExportedAt 불필요(코드 우선 판정). (b) 후속 확인 로컬 알림 — expo-notifications SDK55 API(설치 패키지 타입으로 확인: DATE 트리거 type 필드·shouldShowBanner/List·granted) 기반 `followUpNotifications` 서비스(schedule/cancel/resync, identifier=decision.id 중복방지, Android 채널, 권한요청, 포그라운드 핸들러), v16 `settings.notifications_enabled` + 설정 토글, 확정/수정→sync·반려/회고/삭제→cancel 배선, `_layout` 부트 재동기화 + 응답 리스너 딥링크(/inbox). 인메모리 v1→v16 통과, verify PASS. 알림 발화·탭은 에뮬레이터 검증 필요 | src/services/obsidian/export.ts, src/services/followUpNotifications.ts, src/db/schema.ts, src/db/repos/settings.ts, src/components/settings/NotificationSection.tsx, app/_layout.tsx, app/(tabs)/settings.tsx, src/stores/inbox.ts, app/(tabs)/archive.tsx, src/components/decision/DecisionList.tsx, scripts/migrations.lock.json | 결정 회수 강화(export 필드·Dataview) + 후속 확인 능동 알림 — Decision-Enhancement-Guide D3 |
| 2026-07-03 | D4 구현(의사결정 강화 완결): (a) `getDecisionPerformance`(카테고리별 결과분포·확신도 calibration good율·실행지연 중앙값) + `DecisionStats`(DonutChart/ChartLegend 재사용) /decisions 상단 접이식. (b) `decision_links` 가동 — `decisionLinks` repo(insert/getLinksForDecision 양방향/deleteDecisionLink 하드/getRelatedDecisions), 확정 직후 `SimilarDecisionsSheet`로 유사결정 top3 선택 저장(자동저장 없음), DecisionList 상세 '연관 결정' 표시. (c) `getDecisionsOnThisDay`(getOnThisDay 패턴) + `DecisionOnThisDay` 스트립. (d) tags_json '예약 컬럼' 주석은 v1 마이그레이션 SQL 수정=해시락 위반이라 domain.ts에 기재(코드 우선). 인메모리 v1→v16 13항목 통과, verify PASS | src/db/repos/{stats,decisions,decisionLinks}.ts, src/db/index.ts, src/types/domain.ts, src/components/decision/{DecisionStats,DecisionOnThisDay,SimilarDecisionsSheet,DecisionList}.tsx, app/(tabs)/inbox.tsx, app/decisions.tsx | 축적 회수(대시보드·연관·재부상) — Decision-Enhancement-Guide D4, 지시서 이력 전환 |
| 2026-07-03 | 에뮬레이터 피드백 반영: (1) Archive 검색 중복 수정 — 텍스트 엔트리가 곧 의사결정이면 메모 결과를 제외(결정 카드만), (2) 통계/이날의결정을 Inbox '전체 목록'(DecisionList showInsights)에서 보이게(/decisions는 nav 없는 딥링크였음), (3) `__DEV__` 전용 개발자 도구 카드 신설 — 설정 하단 텍스트 7탭으로 열림. `devSeed`(작년/재작년 오늘 결정·회고·링크·2분뒤 후속알림 시드 + 마커 기반 일괄 제거, FTS 잔여행 정리) + 알림 테스트(10초 발송·예약개수·전체취소·재동기화) + DB 상태. 인메모리 시드/제거 11항목 통과, verify PASS | app/(tabs)/{archive,inbox,settings}.tsx, src/db/repos/devSeed.ts, src/db/index.ts, src/services/followUpNotifications.ts, src/components/settings/DevToolsSection.tsx | 시간 의존 기능 테스트 가능화 + 검색/도달성 버그 수정 |
| 2026-07-03 | 개발자 도구 후속: 시드 텍스트 엔트리를 아카이브 브라우즈 뷰(캘린더·타임라인·이날의기억·검색)에서 숨김 — entries의 4개 조회 + searchTranscripts에 `metadata_json NOT LIKE '%"__seed__"%'` 추가. `getEntry`(단건)는 그대로라 인박스 보드/결정 기능은 정상. 프로덕션엔 마커가 없어 무영향. 인메모리 6항목 통과, verify PASS | src/db/repos/entries.ts, src/db/repos/transcripts.ts | 시드 데이터가 일반 열람에 노출되지 않도록(시드 엔트리 숨김) |
| 2026-07-03 | E2 구현(추출 정밀도): (a) prompts.ts 결정정의에 4번째 기준 '중대성'(되돌리기 어려움/돈·시간/영향 1주+) 추가 + EX2를 취침다짐→중대 daily(PT 3개월)로 교체, 취침다짐은 부정 EX4로 강등(few-shot 4개), buildUserMessage에 hint=0 경계주의 라인. (c) `getRecentRejectedSummaries`(rejected·user우선) + ExtractHints.recentRejectedSummaries + gemini `buildExtractionSystemText` 동적 블록(정적 SoT 불변, 사용자데이터 주입) + labelExtraction 배선. (d) hint 0/1 라인. (b) inbox `LOW_CONFIDENCE_THRESHOLD=0.6` — 덱은 high만, low는 `LowConfidenceCandidates` 접힘 그룹. 인메모리 반려조회 검증, verify PASS | src/services/label/{prompts,gemini,types}.ts, src/services/jobs/handlers/labelExtraction.ts, src/db/repos/decisions.ts, src/components/inbox/LowConfidenceCandidates.tsx, app/(tabs)/inbox.tsx | 과추출 억제(중대성 기준·반려 캘리브레이션·낮은확신 분리) — UserFeedback-Guide E2 |
| 2026-07-04 | E1 구현(옵시디언→SnackShot 수신함 import, 단방향 채널): AI_JOB_TYPE에 obsidian_import + v17(ai_jobs CHECK 재생성 v12패턴 + settings.obsidian_inbox_last_hash) + `readVaultTextFile`(vault.ts, SDK55 File.textSync) + `handleObsidianImport`(Inbox.md 읽기→djb2 해시 중복방어→'---' 블록 파싱→insertTextEntry+label/export 큐잉→저장 후 헤더만 남기고 비우기, 순서 확정) + `enqueueObsidianImport`(hasActiveJob 중복방지) + queue dispatch + `_layout` 부트·포그라운드 복귀 배선. 인메모리 v1→v17 + 파싱/해시 로직 검증, verify PASS. 파일 실제 읽기/쓰기는 에뮬레이터 검증 필요 | src/types/enums.ts, src/services/jobs/errors.ts, src/db/schema.ts, src/db/repos/settings.ts, src/services/obsidian/{vault,inboxImport,index}.ts, src/services/jobs/handlers/{obsidianImport,index}.ts, src/services/jobs/queue.ts, app/_layout.tsx, scripts/migrations.lock.json | 옵시디언 텍스트 유입 채널 신설 — UserFeedback-Guide E1 |
| 2026-07-04 | E3 구현(개인화, E1~E3 완결): (a) `readUserProfile`(vault SnackShot/Profile.md, 주석strip·2000자 절단·warn, 설정토글 없음) + setupSnackShotFolder에 Profile.md 템플릿(존재 시 보존, 프라이버시 헤더). (b) `getRecentDecisionDigest`(최근 7일 confirmed/edited user요약+outcome). `getAiContext`(프로필 항상 + 다이제스트 옵션). gemini 주입 매트릭스 — 추출=프로필O·다이제스트X(E2 반려와 이중주입 금지), compose/rewrite=프로필O·다이제스트O. labelExtraction/compose-decision/textRevision 배선. 인메모리 다이제스트·프로필파싱 + 매트릭스 구조 검증, verify PASS. 실제 API body는 에뮬레이터 검증 | src/services/obsidian/{profile,vault,index}.ts, src/services/label/{context,types,gemini}.ts, src/db/repos/decisions.ts, src/services/jobs/handlers/labelExtraction.ts, app/compose-decision.tsx, src/services/textRevision.ts | 개인화(프로필·최근맥락 주입) — UserFeedback-Guide E3, 지시서 이력 전환 |
| 2026-07-04 | 정기 점검(자동 실행): (1) D1~D4·E1~E3 핵심 심볼 17종 코드 존재 전수 확인 — 전부 배선 정상, (2) archive.tsx 분할 694→535 — `ArchiveCalendarCard`(ko 로케일·캘린더 테마·테이프/종이말림 카드)·`ArchiveTimelineList`(TimelineRow 타입 + `buildTimelineRows` 순수함수 + 타임라인 FlatList) 추출, JSX 이동만(동작 무변경 의도), verify PASS — 에뮬레이터 스모크(월/주/타임라인/검색) 권장, (3) 잘려 있던 `.expo/types/router.d.ts`(32바이트) 복구로 로컬 verify 정상화, (4) 점검 보고서 신설 — 다음 리팩토링 후보(settings 541·entry/[id] 516·repos/decisions 451), @codemap 미부착 8파일, Paperclip 미사용 표면, 다이어리 감성 제안 5건(작성면 괘선·도장 확대·날짜 소인 등, 미채택) | app/(tabs)/archive.tsx, src/components/archive/{ArchiveCalendarCard,ArchiveTimelineList}.tsx, docs/explorations/SnackShot-Periodic-Review-2026-07-04.md, docs/INDEX.md | 대형 화면 컨텍스트 비용↓(화면 추출 패턴 지속) + 계획 문서 구현 검증 |
| 2026-07-09 | 설정 화면 프로필 편집 기능: `ProfileSection`(멀티라인 편집기·저장/불러오기 버튼·글자수·프라이버시 경고, 순수 프레젠테이션) 신설 + obsidian 서비스에 `loadProfileForEdit`(파일→편집기, 주석 제거)·`saveUserProfile`(편집기→Profile.md 전체 덮어쓰기) 추가, Profile.md 안내 헤더를 `vault.ts` `PROFILE_TEMPLATE_HEADER` 단일 상수로 통일(setupSnackShotFolder·저장 공용). settings 포커스 시 현재 Profile.md 본문 자동 로드, 미연결 시 안내만 표시. DB 스키마 변경 없음(Profile.md 파일이 진실원, E3 유지). `npm run verify` PASS, 실제 SAF 읽기/쓰기는 에뮬레이터 검증 필요 | src/components/settings/ProfileSection.tsx, src/services/obsidian/{profile,vault,index}.ts, app/(tabs)/settings.tsx | E3 프로필을 옵시디언에서 직접 편집하지 않고 앱 설정에서 관리(추가·덮어쓰기) |
| 2026-07-09 | Today 사진 촬영 지원(v18): 하단 '영상' 버튼→'촬영'(사진/영상 토글 카메라, `takePictureAsync`, 후속확인 진입 시 영상 우선, 마이크 권한 완화—카메라만 필수). 신규 `entries.mode='photo'` — 마이그레이션 v18(CHECK 확장 테이블 재생성, 20컬럼·인덱스·FTS 트리거 6개 재생성, node:sqlite 인메모리 v1→v18 + photo insert/무효거부/FTS 검증). 사진은 영상과 동일 파이프라인: 다단계 `Image.compress`(L1 2048px/q0.7·L2 1600/0.55·L3 1080/0.45 + 썸네일)·원본 백업·자동관리(sweep에 photo 포함)·용량통계. 처리는 silent와 동일(STT/AI skip, 메모/hint 가능). preview(Image·no duration·mode photo)·엔트리 상세(Image 렌더)·리스트 카드(EntryCard/EntryDiaryItem 사진 태그·duration 숨김)·옵시디언 export/삭제까지 photo 분기. `npm run verify` PASS. 실기기(카메라 촬영·이미지 압축·SAF) 검증 필요 | app/{record,preview,(tabs)/today,entry/[id]}.tsx, src/types/enums.ts, src/db/{schema.ts,repos/{entries,stats}.ts}, src/lib/storage.ts, src/services/{saveCapturedEntry.ts,jobs/handlers/compression.ts,obsidian/{export,vault}.ts}, src/components/{CaptureBar,EntryCard,EntryDiaryItem,SettingsStats,ui/Icon,today/TodayComposer,archive/OnThisDayStrip}.tsx, scripts/migrations.lock.json | Today에서 영상뿐 아니라 사진도 촬영·압축 관리 |
| 2026-07-09 | 프로필 AI 전달 제어(v19): (1) 전역 토글 `settings.profile_ai_enabled`(마이그레이션 v19 additive ADD COLUMN, 기본 1) — 설정 '내 프로필' 섹션에 'AI에 프로필 전달' 스위치. (2) `getAiContext`에 전역 게이트 + 호출별 `includeProfile` 옵션 도입 → 추출·작성·재작성 3경로 모두 단일 지점에서 프로필 주입 제어(readUserProfile은 파일 read 유지). (3) AI 재작성 시트(`TextRevisionSheet`)에 '내 프로필 반영' 스위치 추가 — `recordAiRewrite`·`useTextRevision.rewrite`에 includeProfile 관통, 이번 재작성만 프로필 제외 가능. node:sqlite v1→v19 검증(컬럼·기본값 1·토글·CHECK), `npm run verify` PASS | src/db/{schema.ts,repos/settings.ts}, src/services/label/context.ts, src/services/textRevision.ts, src/components/revision/{useTextRevision,TextRevisionSheet}.tsx, src/components/settings/ProfileSection.tsx, app/(tabs)/settings.tsx, scripts/migrations.lock.json | 프로필의 AI 전달을 전역·재작성 단위로 제어 |
| 2026-07-10 | 카메라 인앱 갤러리(휴대폰 사진 라이브러리): record 화면 좌하단에 최근 사진 썸네일(`GalleryButton`), 탭 시 `DeviceGalleryModal` — expo-media-library로 사진 최신순 그리드(페이지네이션, 중복 id 방어) + 전체화면 스와이프 뷰어(expo-image contentFit contain, getItemLayout). OS 갤러리·파일피커로 나가지 않고 앱 안에서 표시. 사진 권한은 갤러리 탭 시 요청(카메라 진입 시 자동요청 안 함), 허용 후 최근 썸네일 표시·닫을 때 갱신. 녹화 중엔 썸네일 숨김. 사진 라이브러리이므로 mediaType=photo로 한정(영상 제외). DB·스키마 변경 없음. `npm run verify` PASS (부수: 마운트에서 NUL 패딩으로 손상된 생성파일 `.expo/types/router.d.ts`를 유효 구간까지 잘라 복구). 실기기(사진 접근 권한·자산 로드·뷰어 스와이프) 검증 필요 | app/record.tsx, src/components/camera/{GalleryButton,DeviceGalleryModal}.tsx | 스마트폰 기본 카메라처럼 최근 사진 썸네일·인앱 갤러리 열람 |
| 2026-07-10 | 구현 지시서(G·F·H 트랙) 신설: G 생성 정밀도(compose nullable 출구+few-shot 2개+길이 상한 프롬프트·Zod 이중강제+컨텍스트 절제+temp 0.2, rewrite ±30% 앵커, 재수정률 계기판), F 결정 지원 루프(확정 전 유사결정 노출 PastDecisionsSheet, learnings digest·compose 주입, user_confidence vNEXT+calibration 전환, unclear/mixed 재후속, F5 미결 상태는 ADR 초안까지만), H 투자 확장(H0 추천 금지 경계, structured_json 매매 필드, Profile.md 매매원칙 대조, 증권앱 캡처 파싱—산술 크로스체크·자동저장 금지·원본 미보관, 일봉 시세 QuoteService+quotes 캐시+quote_fetch 잡). 권장 순서 G→F→H | docs/explorations/SnackShot-DecisionSupport-Investment-Guide.md, docs/INDEX.md | 3주제 평가 세션 결론을 후속 구현 세션이 재설계 없이 실행 가능하도록 고정 |
| 2026-07-10 | 지시서 보강: Gemini 프롬프트 전문 확정(부록 A~C — 개정 compose·원칙대조·캡처파싱, 그대로 붙여넣기 지시) + 코드 검토 발견 2건을 G4·G5로 신설 — G4 추출 프롬프트 정비('세 요건' 모순 문구·길이 상한·Zod max), G5 Whisper prompt 죽은 파라미터 가동. CEX1 few-shot 전문·H1 커스텀 카테고리 경계·F3 관통 경로·F2 주입 매트릭스 확정 보강 | docs/explorations/SnackShot-DecisionSupport-Investment-Guide.md | 프롬프트 문안을 구현 세션 재량에 남기지 않음(길이감 자체가 설계) |
| 2026-07-10 | G1+G4+G2 구현(생성 정밀도): (G1) compose 프롬프트 개정 — 길이 상한(40/80/60/100/80자)·"근거 있는 내용만, 모르면 null"·표현 보존, few-shot CEX1(절제 앵커)/CEX2(null 출구 시연)+gemini 배선, COMPOSE_JSON_SCHEMA·ComposeDraftSchema 4필드 nullable+Zod max, DecisionDraft null 허용+화면 `?? ''` 처리, appendComposeContext 절제 1줄, temp 0.4/0.6→0.2/0.4. (G4) 추출 '세→네 요건' 모순 수정+길이 상한+DecisionCandidateSchema Zod max(evidence 제외). (G2) rewrite ±30% 앵커. verify PASS. Gemini 실호출 육안은 에뮬레이터 이월. (부수: 파일동기화 잘림 → git HEAD 복원+재적용으로 해결) | src/services/label/{prompts,schema,gemini,types}.ts, app/compose-decision.tsx | 지시서 G 트랙 착수 — compose 지어내기·장황 억제(구조 원인 제거) |
| 2026-07-10 | G5+G3 구현(생성 정밀도 G 트랙 마무리, 스키마 무변경): (G5) Whisper 전사 프롬프트 가동 — `handlers/stt.ts`가 한 번도 전달하지 않던 죽은 파라미터 `TranscribeOptions.prompt`를 `STT_STYLE_PROMPT`(짧은 스타일·어휘 힌트) 상수로 `transcribe()`에 전달, 무음·극단 짧은 오디오에서 프롬프트 문구가 전사로 새는 알려진 환각을 `result.text.includes(STT_STYLE_PROMPT)`→skipped+warn 로그로 방어(기존 temperature=0 가드와 병행). (G3) 재수정률 계기판 — `getAiRewriteReeditRate`(text_revisions에서 source=ai_rewrite 이후 24h 내 같은 target·field에 source=manual 재수정 있는 비율) repo 쿼리 신설 + DevToolsSection 진단에 'AI 재작성 재수정률' 버튼. node:sqlite로 SQL 로직 검증(윈도우 밖·재작성 이전 manual 제외 확인, total=4/reedited=1). `npm run verify` PASS. 정상/무음 클립 전사·프롬프트 누출은 에뮬레이터 검증 필요. (부수: 세션 파일동기화가 편집을 기존 파일 크기로 잘라 stt.ts·DevToolsSection·textRevisions가 마운트에서 truncate됨 → 전체 내용을 bash로 재기록해 복구) | src/services/jobs/handlers/stt.ts, src/db/repos/textRevisions.ts, src/components/settings/DevToolsSection.tsx | DecisionSupport-Investment-Guide G 트랙 마무리(G5 죽은 파라미터 가동·G3 프롬프트 개선 계기판) |
| 2026-07-10 | F1 구현(확정 전 유사 결정 노출, 스키마 무변경): 검토 시점에 축적된 과거가 개입하도록 — `getSimilarPastDecisions`(decisions_fts+buildFtsQuery 재사용, confirmed/edited·최신 outcome 1건 LEFT JOIN, {decision,result,learnings}[], ORDER BY rank, FTS오류 try/catch 빈배열) repo 신설 + `SimilarPastItem` 타입. inbox store `loadInbox`가 pendingCandidates 각각에 `similarPast` 부착(excludeEntryId·limit3, 실패 무시). 신규 `PastDecisionsSheet`(열람 전용 바텀시트+`SimilarPastBadge` 요약칩, 결과 이모지 카운트·교훈 표시, 저장/링크 없음 — 확정후 SimilarDecisionsSheet와 역할 분리) → DecisionDeck 카드·LowConfidenceCandidates 행에 배지, 탭 시 시트. node:sqlite로 조인/최신outcome/ORDER BY rank/제외 검증. `npm run verify` PASS. 덱 표시·시트 열람은 에뮬레이터 검증(devSeed 유사결정 활용). (부수: 세션 파일동기화가 편집 5파일을 기존 크기로 truncate → git HEAD+편집 재적용으로 bash 재기록 복구, NUL 손상된 생성파일 `.expo/types/router.d.ts` 유효 스텁으로 복구) | src/db/repos/decisions.ts, src/stores/inbox.ts, src/components/decision/PastDecisionsSheet.tsx, src/components/DecisionDeck.tsx, src/components/inbox/LowConfidenceCandidates.tsx, app/(tabs)/inbox.tsx | DecisionSupport-Investment-Guide F1 — 결정 지원 루프 착수(과거가 새 결정에 개입) |
| 2026-07-10 | F2 구현(learnings 회수 루프, 스키마 무변경): (a) `getRecentDecisionDigest` SELECT에 `o.learnings` 추가 + `DecisionDigestItem.learnings`, gemini `formatDigestItem`이 교훈 있으면 ` — 교훈:`(80자 절단) 덧붙임. (b) compose에 과거 유사 교훈 주입 — `AiContext.relevantLearnings?: string[]` 신설, `compose-decision.tsx` handleFill이 `getAiContext` 후 `getSimilarPastDecisions(seed, {limit:5})`의 learnings만 추려 `{...context, relevantLearnings}`로 전달, `appendComposeContext`가 '## 과거 유사 결정에서 얻은 교훈' 블록 추가(있을 때만). 주입 매트릭스: compose만 — 추출은 buildExtractionSystemText(무관), rewrite는 relevantLearnings 미세팅(textRevision.ts 무변경)이라 자연 제외(JS 미러로 compose=포함·rewrite=미포함 검증). `npm run verify` PASS. 요청 body 교훈 블록·매트릭스는 에뮬레이터 검증 이월 | src/db/repos/decisions.ts, src/services/label/gemini.ts, src/services/label/types.ts, app/compose-decision.tsx | DecisionSupport-Investment-Guide F2 — learnings 회수(digest·compose 주입) |
| 2026-07-10 | F3 구현(사용자 확신도 · v20 마이그레이션): (1) v20 `ALTER TABLE decisions ADD COLUMN user_confidence REAL`(additive, CHECK 재생성 불필요) + toDecision 카탈로그·domain.ts `Decision.userConfidence?` + lock 갱신(node:sqlite v1→v20 컬럼·null기본·set 검증). (2) 공용 `ConfidenceChips`(50·60·70·80·90%, 0~1 저장, 재탭 해제) — EditDecisionSheet '5. 확신도'·compose '확신도' 필드. `EditParams`·store confirm/edit·`updateUserEdit`(COALESCE)·`saveAuthoredDecision`·`insertDecision`(user_confidence 컬럼)까지 시그니처 관통(tsc 강제). (3) calibration 전환 — `getDecisionPerformance` 버킷을 `COALESCE(user_confidence, confidence)`로(node:sqlite로 user 우선 검증), DecisionStats에 '본인 입력 확신도 우선' 캡션, OutcomeEditor '당시 기대' 확신도도 `userConfidence ?? confidence`. `npm run verify` PASS(20 versions). 칩 입력→저장→통계 반영은 에뮬레이터(devSeed) 이월 | src/db/{schema.ts,repos/{decisions,stats}.ts}, src/types/domain.ts, src/stores/inbox.ts, src/services/saveAuthoredDecision.ts, src/components/decision/{ConfidenceChips,DecisionStats}.tsx, src/components/{EditDecisionSheet,OutcomeEditor}.tsx, app/compose-decision.tsx, scripts/migrations.lock.json | DecisionSupport-Investment-Guide F3 — 본인 판단력 측정(user_confidence) |
| 2026-07-10 | F4 구현(재후속 루프, 스키마 무변경): 회고 결과가 unclear/mixed면 `recordOutcome` 직후 inbox 화면에서 '아직 판단하기 이르다면 7일 뒤 다시 물어볼까요?' Alert 제안(good/bad엔 제안 없음) → 수락 시 store `reFollowDecision`. **가이드 대비 편차(코드 우선·기록)**: 가이드가 명시한 'follow_up_at 갱신+resync' 2개 mutation만으로는 recordOutcome이 남긴 `executed_at`+outcome 때문에 보드/알림 어디에도 재등장·재예약이 불가(getDecisionsDueForFollowUp·shouldSchedule이 둘 다 배제) → 코드 정합을 위해 잠정 판단 되돌리기를 추가: 최신 outcome soft-delete(getOutcomeByDecision+softDeleteOutcome 재사용) + `unmarkDecisionExecuted` + 신규 `setDecisionFollowUp`(follow_up_at=+7일·set_by='refollow', status 미변경) + `resyncFollowUpNotifications`. 결과: 즉시 '진행 중' 복귀·7일 뒤 '후속 확인' 재등장(node:sqlite 상태전이 검증: before 미표시→after upcoming 즉시·due는 +8d, setBy=refollow·executed=null). `npm run verify` PASS. Alert 발화·알림 재예약 개수는 에뮬레이터(devSeed) 이월 | src/db/repos/decisions.ts, src/stores/inbox.ts, app/(tabs)/inbox.tsx | DecisionSupport-Investment-Guide F4 — 재후속 루프(잠정 판단 되돌려 재확인) |
| 2026-07-10 | F1~F4 구현 검토(사후 검토): 핵심 배선 전수 확인 — F1(최신 outcome 조인·rank·배지/시트), F2(digest 교훈 80자·compose 전용 주입), F3(v20 additive·카탈로그·관통·calibration COALESCE), F4(상태전이·resync) 모두 정상. 결함 1건 수정: 회고/교훈을 적은 unclear/mixed outcome에서 재후속 수락 시 사용자 텍스트가 soft-delete로 소실 → 메모 있으면 재후속 제안 생략(proposeRefollow hasMemo 게이트). 부수: 동기화 잘림으로 파편화된 CLAUDE.md 이력 3행 복구. verify PASS(20 versions) | app/(tabs)/inbox.tsx, CLAUDE.md, docs/explorations/SnackShot-DecisionSupport-Investment-Guide.md | F 트랙 품질 게이트 + 사용자 텍스트 보호 |
| 2026-07-10 | F5 ADR 초안 신설(구현 없음): 미결(deliberating) 결정 — status 확장 선택(별도 테이블·플래그 컬럼 대안 비교: 화이트리스트 쿼리 덕에 오염 불가능·전이 시 ID 연속), decide_by 마감 컬럼+shouldSchedule 분기 알림, 대안은 기존 alternatives 재사용(선택지별 구조화 미채택, structured_json은 매매 전용 유지), '결정했다' 플로우에 F1 PastDecisionsSheet 자동 표시, 생성은 compose 토글만(AI 추출발 미결 제외 — E2 과추출 재발 방지), 화면 영향 전수표(통계·digest·export는 자동 제외 확인). H4 이벤트 대기 매매가 첫 실사용처. 승인 후 ADR 반영→구현 | docs/explorations/ADR-deliberating-draft.md, docs/INDEX.md | DecisionSupport-Investment-Guide F5 — ADR 임의 변경 금지 준수(초안→승인 절차, ADR-005 개정 선례) |
| 2026-07-10 | F5 구현(미결 deliberating 결정 · ADR-028 · v21): 초안 승인 → ADR-028 반영(SnackShot-ADR.md·INDEX). status에 'deliberating' 추가(enums.ts 먼저) + v21 decisions **테이블 재생성**(CHECK 확장 + `decide_by INTEGER`, decisions_fts 트리거 3개 DROP/재생성=D1 함정, 26컬럼 명시 매핑·인덱스4 재생성, node:sqlite v1→v21: FTS보존·행보존·CHECK·트리거6·무효status거부·인덱스4 검증). mapping/domain `decideBy`. 생성=의도적 작성만: compose '아직 결정 못 했어요' 토글(summary만 필수·마감 N일 입력) → saveAuthoredDecision deliberating 분기(status=deliberating·confirmed_at 없음·export 제외·decide_by 알림 sync). 보드 '고민 중' 섹션(`DeliberatingCard`, 마감 D-n/지남 강조) + '결정했다'(F1 PastDecisionsSheet 자동→EditDecisionSheet 확정 전이: updateUserEdit+`setDecisionConfirmedNow`)/'접기'(soft delete). `getDeliberatingDecisions`·`setDecisionConfirmedNow` repo, `getAllDecisions`에 deliberating 포함 + DecisionList '미결' 배지. followUpNotifications shouldSchedule/scheduleFor 미결 분기(제목 '결정할 시간', decide_by 트리거)·resync 순회 추가. devSeed 미결 2건(임박·경과). 통계·digest·유사검색은 화이트리스트라 자동 제외(node:sqlite 오염없음 검증). `npm run verify` PASS(21 versions). 화면 조작·알림 발화는 에뮬레이터(devSeed) 이월 | SnackShot-ADR.md, src/types/{enums,domain}.ts, src/db/{schema.ts,repos/{decisions,devSeed}.ts}, src/services/{saveAuthoredDecision,followUpNotifications}.ts, src/stores/inbox.ts, src/components/{inbox/DeliberatingCard,decision/DecisionList}.tsx, app/{compose-decision,(tabs)/inbox}.tsx, scripts/migrations.lock.json | DecisionSupport-Investment-Guide F5 — 미결 1급 도입(ADR-028), H4 이벤트 대기 매매 기반 |
| 2026-07-10 | H1 구현(매매 구조화 필드 · v22): `structured_json TEXT` additive ADD COLUMN(v22, decisions 재생성 불필요) + domain `structuredJson`·mapping 카탈로그·insertDecision(27컬럼)·saveAuthoredDecision 관통. 신규 `src/services/trade/schema.ts`(TradeDetailsSchema Zod: kind/name/ticker/side/amountKrw/quantity/entry·target·stopPrice/eventTrigger/priceAtDecision, parseTradeDetails safeParse, formatTradeDetails·tradeExportLines). compose 화면에 category='investment'일 때만 접이식 `TradeFieldsSection`(자체 상태→onChange TradeDetails|null, name 비면 null) → `JSON.stringify`로 structured_json 저장. DecisionList 상세·옵시디언 buildDecisionCallout에 매매 필드 표시/기재(있을 때만). 음성 추출 경로 무변경(H0·지어내기 방지). node:sqlite v1→v22 컬럼·JSON 왕복 검증, `npm run verify` PASS(22 versions). 화면 입력·export 육안은 에뮬레이터 이월 | src/db/{schema.ts,repos/decisions.ts}, src/types/domain.ts, src/services/{trade/schema.ts,saveAuthoredDecision.ts,obsidian/export.ts}, src/components/decision/{TradeFieldsSection,DecisionList}.tsx, app/compose-decision.tsx, scripts/migrations.lock.json | DecisionSupport-Investment-Guide H1 — 투자 확장 착수(매매 정량 기록) |
| 2026-07-10 | H3 구현(증권앱 캡처→포트폴리오 파싱 · v23): `portfolio_snapshots` 신규 테이블(source CHECK image/manual·holdings_json·soft delete + created_at 인덱스) + `src/db/repos/portfolio.ts`(insert/getLatest/list/softDelete). `src/services/trade/portfolio.ts`(HoldingSchema·PortfolioParseSchema Zod 전수치 nullable, `flagHoldingReview` 산술 크로스체크 — |수량×평단−매입|/매입>2% 또는 |수량×현재가−평가|/평가>2%면 needsReview, 해당 필드 모두 있을 때만). `PORTFOLIO_PARSE_SYSTEM_PROMPT`(부록C 그대로) + `PORTFOLIO_JSON_SCHEMA` + gemini `parsePortfolioImage`(멀티모달 inlineData/mimeType camelCase·temp0.1·Zod 검증) + LabelService 인터페이스. `/portfolio-import` 화면 — expo-image-picker 미설치·네트워크 차단으로 expo-media-library(설치됨) 선택→`File.base64()`→파싱→편집 가능 확인 행(needsReview 경고색)→저장, 원본 이미지 미보관 + 상단 고정 고지. 진입점: DecisionList 통계 하단 버튼→/portfolio-import. node:sqlite v1→v23·크로스체크(정상/불일치/허용오차/누락) 검증, `npm run verify` PASS(23 versions). 실제 이미지 픽·base64·Gemini 응답은 에뮬레이터/실기기 이월 | src/db/{schema.ts,index.ts,repos/portfolio.ts}, src/services/{trade/portfolio.ts,label/{prompts,schema,types,gemini}.ts}, src/components/decision/DecisionList.tsx, app/portfolio-import.tsx, scripts/migrations.lock.json | DecisionSupport-Investment-Guide H3 — 포트폴리오 캡처 파싱(코드 검산 안전장치) |
| 2026-07-10 | H2 구현(매매 원칙 대조, 스키마 무변경): `PRINCIPLE_CHECK_SYSTEM_PROMPT`(부록B — 투자자문 아님·충돌만·권유 금지) + `buildPrincipleCheckMessage`(섹션 순서 고정: 매매원칙/결정/매매정보/현재포트폴리오) + `PrincipleCheckSchema`·`PRINCIPLE_CHECK_JSON_SCHEMA`(conflicts:[{rule,issue}]) + LabelService `checkPrinciples`(gemini temp0.2, Profile.md 전문 주입·최신 포트폴리오 스냅샷 있으면 함께). compose 화면에 category=investment·매매정보 입력·vault 연동일 때만 '내 매매 원칙과 대조' 버튼 → 결과 인라인 표시(충돌 rule/issue 경고색, 없으면 '충돌 없음'). **저장 미차단**(H0/확정 — 어기는 것도 본인 선택, 기록이 가치). readUserProfile·getLatestPortfolioSnapshot 재사용. `npm run verify` PASS(스키마 무변경). 실제 Gemini 충돌 판정·추천문구 미출력은 에뮬레이터 이월 | src/services/label/{prompts,schema,types,gemini}.ts, app/compose-decision.tsx | DecisionSupport-Investment-Guide H2 — 원칙 대조(Profile.md 재사용) |
| 2026-07-10 | H4 구현(시세 서비스 · v24, H 트랙 완결): 범위=일봉 종가만(실시간·호가·차트 없음, H0 경계). v24 `quotes`(ticker,date PK 캐시) + `AI_JOB_TYPE`에 'quote_fetch'(enums.ts 먼저) → ai_jobs CHECK 재생성(v17 패턴, 트리거 없음·인덱스2 재생성, node:sqlite v1→v24: 기존 잡 보존·quote_fetch 수용·무효 거부·quotes upsert 검증). `QuoteService` 인터페이스(ADR-008) + 공공데이터포털 금융위 구현(`getQuoteApiKey` env·SecureStore) + `getDailyCloseCached`(캐시 우선). `handleQuoteFetch`(payload ticker/date→캐시/조회→decision.structured_json.priceAtDecision 없을 때만 기입, 키·티커·데이터·실패 전부 skip=치명 아님) + queue dispatch + handlers 배럴. saveAuthoredDecision이 매매(ticker) 저장 시 quote_fetch enqueue. `TradeQuoteCompare`(OutcomeEditor 당시기대 블록 — 진입가 vs 현재 종가 %, 조회 1회 캐시우선, 실패 시 수동 입력 폴백, async/await). `updateDecisionStructuredJson` repo. `npm run verify` PASS(24 versions). 실제 공공데이터 API 응답·키·시세 대조는 실기기 이월(네트워크 제한 환경 미검증) | src/types/enums.ts, src/lib/env.ts, src/db/{schema.ts,index.ts,repos/{quotes,decisions}.ts}, src/services/{quotes/*,jobs/{queue,errors,handlers/{index,quoteFetch}}.ts,saveAuthoredDecision.ts}, src/components/{OutcomeEditor,decision/TradeQuoteCompare}.tsx, scripts/migrations.lock.json | DecisionSupport-Investment-Guide H4 — 시세 대조(일봉), H 트랙 완결 |
| 2026-07-10 | F5·H1~H4 구현 검토(사후 검토): 전수 확인 — v21~v24 마이그레이션(FTS 트리거 선제 DROP/재생성·26컬럼 명시 매핑·인덱스 재생성·ai_jobs v17 패턴), H2/H3 프롬프트 부록 B·C 원문 일치, 산술 크로스체크(2%·0나눗셈 가드), 화이트리스트 오염 없음(통계·digest·유사검색·export), 알림 미결 분기·resync 순회, H0 경계(추천 금지·저장 미차단·원본 미보관·수동 폴백) 모두 정상. 결함 1건 수정: `decideDeliberating`(미결→확정 전이)이 옵시디언 export를 큐잉하지 않아 화면 obsidianPrompt 안내와 불일치(확정 결정이 vault 미도달) → 전이 시 maybeEnqueueReExport 배선. verify PASS(24 versions) | src/stores/inbox.ts, CLAUDE.md | F5·H 트랙 품질 게이트 — 전이 시점 export 정합 |
| 2026-07-10 | H5·H6 구현 지시서 추가: H5 시세 UX(설정 키 입력 UI 배선—죽은 export 가동, getDailyClose 정렬 비의존 교정—범위 조회+srtnCd 정확 필터+basDt 최대, QuoteService에 getDailyCandles/searchSymbols 확장, TickerSearchField 종목명 검색·DailyQuotesPanel 30일 라인차트+일봉 리스트+행 탭 원탭 기입—svg 선례 확인, 캐시 안 함 확정, T+1 고지), H6 미국 주식(Twelve Data 구현체—HTTP 200 오류·문자열 숫자 함정 명시, /^\\d{6}$/ 티커 라우팅으로 핸들러·화면 무변경, 한글 쿼리 KRX 한정, 통화 필드 미채택). H4 범위 문구 개정(일봉 열람·기입 보조 허용, H0 무변경). 키 2종 발급·.env 적용은 완료 상태 명시 | docs/explorations/SnackShot-DecisionSupport-Investment-Guide.md, docs/INDEX.md, .env | 시세 실사용 UX(검색·열람·원탭 기입)와 미국 주식 지원을 재설계 없이 구현 가능하도록 고정 |
| 2026-07-10 | H5 구현(시세 UX · 스키마 무변경): 범위 개정(승인) — '일봉 열람·기입 보조 허용, 실시간·호가·분봉·지표 없음', H0 무변경. (a) 설정 'API 키' 섹션에 '시세 API (공공데이터포털)' KeyInputRow — `KeyInputRow` models optional화(모델 없는 키), `setQuoteApiKey`/`deleteQuoteApiKey` 배선(죽은 export 가동). (b) `dataPortal.getDailyClose` 정렬 비의존 교정 — beginBasDt=조회일-14d·endBasDt=조회일·numOfRows20 범위조회 후 `srtnCd===ticker` 정확필터 + basDt 최대 선택. (c) `QuoteService`에 `getDailyCandles`(days×1.6 범위·오름차순·mkp/clpr/hipr/lopr)·`searchSymbols`(likeItmsNm·srtnCd dedupe·상위10) 확장, 캐시 안 함(열 때마다 1콜). (d) `TickerSearchField`(명시 검색 버튼→결과 탭 자동 기입, 키 없으면 버튼 숨김)·`DailyQuotesPanel`(CollapsibleSection lazy 마운트로 open 시 1콜, 30일 종가 min-max 라인차트 svg Polyline+일봉 리스트+행탭 ActionSheet 원탭 기입 진입/목표/손절+T+1 고지) 신설, TradeFieldsSection 통합(name/ticker→TickerSearchField, 하단 패널). JS 미러로 정확필터·basDt최대·dedupe·정렬슬라이스 검증, `npm run verify` PASS(스키마 무변경). 실키 검색·차트·주말 직전거래일·SecureStore 경로는 실기기 이월 | src/components/settings/KeyInputRow.tsx, app/(tabs)/settings.tsx, src/services/quotes/{types,dataPortal}.ts, src/components/decision/{TickerSearchField,DailyQuotesPanel,TradeFieldsSection}.tsx | DecisionSupport-Investment-Guide H5 — 시세 실사용 UX(검색·열람·원탭 기입) |
| 2026-07-10 | H6 구현(미국 주식 Twelve Data + 티커 라우팅, 스키마 무변경): env `twelveData` 키(get/set/delete, __DEV__ .env fallback) + 설정 '미국 시세 (Twelve Data)' KeyInputRow. `twelveData.ts`(time_series·symbol_search, 함정 3종 처리 — status:'error'가 HTTP200으로 오는 것 검사·문자열 숫자 parseFloat·한도초과 조용 skip). `quotes/index.ts` 라우터 — `/^\d{6}$/` KRX(dataPortal) else 미국(twelveData), 핸들러·화면·getDailyCloseCached 무변경(라우터 흡수). 검색은 한글 쿼리→KRX 전용, 그 외→두 소스 병행 후 KRX 먼저 합침(개별 실패 무시). TradeFieldsSection 검색게이트를 any-key(KRX∥Twelve)로 확장. **편차(코드 우선·기록)**: ① Twelve Data 엔드포인트·응답필드(values[]/data[]/status:error)는 네트워크 차단으로 공식문서 미확인·학습데이터 가정 — 실기기서 다르면 코드 우선 교정 필요(가이드 (c) 지침). ② datetime을 KRX와 통일하려 'yyyy-MM-dd'→'yyyyMMdd' 변환(가이드 미명시, DailyCandle.date 공용 형식 일관성). JS 미러로 라우팅(KRX/US·한글/영문검색) 검증, `npm run verify` PASS. 실 API·키·미국 종목 조회는 실기기 이월 | src/lib/env.ts, app/(tabs)/settings.tsx, src/services/quotes/{twelveData,index}.ts, src/components/decision/TradeFieldsSection.tsx | DecisionSupport-Investment-Guide H6 — 미국 주식 지원(티커 라우팅) |
| 2026-07-10 | H5·H6 완료 검토: (편차 종합) H5 — KeyInputRow를 models optional로 확장(시세 키는 모델 없음, 기존 Gemini/OpenAI 행 무변경)·30일 차트는 TrendLineChart(baseline0 부적합) 대신 DailyQuotesPanel 내 min-max Polyline 신설(가이드 'svg Polyline' 범위 내)·검색게이트 any-key. H6 — Twelve Data 스펙 학습데이터 가정(문서 미확인, 코드 우선 원칙)·datetime yyyyMMdd 통일. (파일 잘림 확인) H5·H6 편집 10파일 전수 tail 검사 — 전부 정상 종료 토큰, NUL 바이트 0, tsc exit 0. 이번 2 phase는 마운트 truncation 재발 없음 | — (검토 기록) | 사용자 요청(편차 기록 + 파일 잘림 확인) |
| 2026-07-11 | IA 개편 목업 신설(I 트랙 방향 합의용): 사용자 피드백(주식 접근성·시인성 부족, todo/결정/과거/유사 구분 곤란) → 진단(기능 누적 대비 정보구조 한계 — 시트·배지 파편화, 결정의 홈 부재, 투자 4곳 분산). 시안: 5탭(Today·Archive·Inbox처리함·결정·투자), I1 결정 상세 화면(/decision/[id] — 유사과거 상시·회고 타임라인·원본영상·매매블록), I3 투자 탭(포트폴리오 평가·원칙 상시대조·종목 일봉차트에 ▲▼◇ 결정 마커 오버레이), I2 Inbox 축소+결정 탭, I4 카드 시각언어(카테고리색·origin·티커칩·미결스탬프). 구현 순서 I1→I3→I2→I4, 신규 스키마 불필요(기존 데이터 재조합). 승인 시 지시서화 | docs/explorations/SnackShot-IA-Overhaul-Mockup.html, docs/INDEX.md | 기능(D~H) 완비 후 정보구조 재편 — 축적물의 접근성·시인성 회복 |
| 2026-07-11 | 종목 검색 피드백 개선(사용자 버그 리포트 — 검색 성공/실패 구분 불가): 서비스 계층에 strict 모드 도입 — fetchItems/tdFetch가 대화형 경로에서만 키 없음·HTTP·네트워크·API 오류를 throw(백그라운드 자동 기입은 기존대로 조용, H0 유지), 라우터 병행 검색은 allSettled로 한쪽 실패=부분 결과·양쪽 실패=throw. TickerSearchField 4상태 피드백 — '검색 중'·'검색 결과 없음(한글은 국내만)'·'조회 실패(네트워크·키 확인)' feedback.danger 표시·키 미등록 시 안내 캡션(기능 숨김→안내로 전환). verify PASS. (부수: NUL/잘림 손상 재발한 .expo/types/router.d.ts를 유효 스텁으로 복구 — 선례 동일) | src/services/quotes/{dataPortal,twelveData,index,types}.ts, src/components/decision/TickerSearchField.tsx | 대화형 액션은 0건과 오류를 구분해야 함(조용 원칙은 백그라운드 한정) |
| 2026-07-11 | I 트랙 구현 지시서 신설(목업 승인 후속): I1 결정 상세 /decision/[id] — 유사과거 상시 섹션·회고 타임라인(getOutcomeHistory, soft-deleted outcome을 '잠정 판단(되돌림)'으로 열람)·매매 블록·진입점 통일(목록/보드/검색 탭=상세, 덱 제외·빠른 액션은 카드 유지). I3 투자 탭+종목 상세 — vNEXT(portfolio_snapshots ADD principle_check_json), 원칙 상시 대조 principleWatch(Gemini 1회+해시 캐시, 실패 조용), 평가액(캐시 우선+캡처값 폴백), 일봉 차트 ▲▼◇ 결정 마커(휴장일 다음 거래일 스냅, 마커 탭 미채택—행 탭), 종목 교훈 모음. I2 5탭(Today·Archive·Inbox·결정·투자)+설정→Today 헤더 ⚙ 스택 이동, Inbox=후보·후속·마감 도래 미결만(배지 정합·딥링크 전수 확인). I4 카테고리 색 토큰·origin 아이콘·티커+등락 칩(quotes 캐시만)·확신 병기·미결 스탬프 공용화. 순서 I1→I3→I2→I4, 세션당 1 phase·에뮬레이터 필수·잘림 확인 특칙 | docs/explorations/SnackShot-IA-Overhaul-Guide.md, docs/INDEX.md | 승인된 목업을 재설계 없이 구현 가능하도록 고정(실현성 매트릭스 반영 — 유일한 신규 설계는 원칙 상시 대조) |
| 2026-07-14 | I1 구현(결정 상세 화면 /decision/[id], 스키마 무변경): 시트·카드 확장에 흩어진 결정 맥락을 한 화면으로. 신규 `getOutcomeHistory`(outcomes, deleted_at 포함 전체 created_at DESC — F4 soft-delete가 남긴 잠정 판단 열람용) repo. 화면 조립 + 섹션 4종을 `src/components/decision/detail/`로 분리(DecisionDetailHeader 칩행[카테고리·상태·확신 userConfidence??confidence·origin🎙/✍·티커], DecisionDetailBody 상황/이유/대안+매매블록[formatTradeDetails+TradeQuoteCompare]+연관결정+원본보기/수정, SimilarPastSection 유사과거 상시[없으면 "아직 비슷한 기록 없음"], OutcomeTimeline 고정이벤트[기록·확정·수행]+회고 병합 최신순·되돌림 라벨). 편집=EditDecisionSheet 재사용, 미결 '결정했다'=inbox store 플로우 재사용(loadInbox로 보드 state 채운 뒤 decideDeliberating — 재export 정합). 진입점 통일: DecisionList 인라인 확장 **제거**→push(+onThisDay·edit sheet 제거로 dead code 정리), inbox 진행중 보드카드 onPress→push(빠른액션 유지), archive 검색 결정카드 onPress→push. **편차(코드 우선·기록)**: ① 가이드 진입점 목록의 '보드 카드'는 body-tap edit sheet가 있던 DecisionBoardCard(진행중)만 해당 — FollowUp/DoneRow/Deliberating은 본문탭이 처리액션(메모·후기·결정했다)이라 유지(가이드 "빠른액션은 카드에" 준수). ② onThisDay 탭도 상세로 통일(가이드 미명시나 dead edit sheet 방지). `npm run verify` PASS(24 versions). 화면 진입·미결전이·타임라인 육안은 에뮬레이터 이월. (부수: 세션 파일동기화가 archive.tsx를 532줄로 mid-line truncate → git show HEAD:로 535줄 복원 후 236행 edit 재적용, 선례 동일) | src/db/repos/outcomes.ts, app/decision/[id].tsx, src/components/decision/detail/{DecisionDetailHeader,DecisionDetailBody,SimilarPastSection,OutcomeTimeline}.tsx, src/components/decision/DecisionList.tsx, app/(tabs)/{inbox,archive}.tsx, docs/CODEMAP.md, docs/explorations/SnackShot-IA-Overhaul-Guide.md | IA 개편 I1 — 결정의 홈(상세 화면) 신설, 모든 결정 카드의 착지점 통일 |
| 2026-07-14 | I3 구현(투자 탭 + 종목 상세 · v25 마이그레이션): (a) v25 `ALTER TABLE portfolio_snapshots ADD COLUMN principle_check_json TEXT`(additive) + PortfolioSnapshot.principleCheckJson·toSnapshot 매핑(SnapshotRow 타입)·`updateSnapshotPrincipleCheck` repo. node:sqlite v1→v25 컬럼·JSON 왕복·신규 스냅샷 NULL 검증. (b) `src/services/trade/principleWatch.ts` — 최신 스냅샷+Profile.md 원칙 로드→djb2 해시(E1 선례)를 스냅샷 캐시와 비교→같으면 재사용, 다르거나 새 스냅샷(NULL)이면 `checkPrinciples`(H2) 1회 후 스냅샷 행에 캐시. 실패·키없음·vault미연동·프로필없음→null(섹션 숨김, 조용, H0 표시만). (c) `app/invest.tsx` 스택 라우트(I2에서 탭 승격) — PortfolioCard(평가액=Σ quantity×최신종가 `computePortfolioValuation`[valuation.ts, getDailyCloseCached 캐시우선+valuationAmount 폴백+제외 캡션])·StockDecisionList(`getTradeDecisionRows` 전량+호출자 ticker 그룹핑)·PrincipleWatchSection(Profile.md '## 매매 원칙' 원문 추출+conflicts 경고카드). (d) `app/stock/[ticker].tsx` — StockDecisionChart(90일 종가 Polyline+▲매수/▼매도/◇미결 마커, 기준일 이분탐색 휴장일 다음거래일 스냅·범위밖 생략·decide_by 미래는 우측 점선, react-native-svg G/Polygon/Line, 마커 탭 미채택)·이 종목 결정 리스트(행 탭→I1 상세)·교훈 모음. 진입점: DecisionList 하단 '📷 캡처' 버튼을 '📊 투자 현황'→/invest로 이관(I2 탭 승격 전 임시 진입), portfolio-import 저장 직후 runPrincipleWatch 1회(fire-and-forget 캐시 워밍). **편차(코드 우선·기록)**: ① H2 checkPrinciples는 단일 매매 대조용(tradeDetails 필수)이라 포트폴리오 전반 점검엔 자리표시 tradeDetails(side:hold)를 넘기고 실제 컨텍스트는 portfolio로 전달. ② 결정 종목 리스트의 '등락 칩'은 quotes 캐시 전용이라 I4 카드 시각언어로 이월(I3 리스트는 종목명·결정 N·미결 n만). ③ DecisionList 포트폴리오 진입점은 '제거'가 아니라 /invest로 '이관'(스택 라우트 도달성 확보, I2에서 탭으로 대체). `npm run verify` PASS(25 versions). 평가액·원칙 대조·차트 마커 육안은 에뮬레이터/실기기 이월. (부수: 세션 파일동기화가 편집 5파일[DecisionList·decisions·portfolio repo·trade/portfolio·portfolio-import]을 NUL 주입·mid-line truncate → git show HEAD:로 복원 후 편집 재적용, DecisionList는 file-tool Write도 재손상되어 bash heredoc으로 기록, 선례 동일) | src/db/{schema.ts,repos/{portfolio,decisions}.ts}, src/services/trade/{portfolio,principleWatch,valuation}.ts, app/{invest,stock/[ticker],portfolio-import}.tsx, src/components/invest/{PortfolioCard,StockDecisionList,PrincipleWatchSection,StockDecisionChart}.tsx, src/components/decision/DecisionList.tsx, scripts/migrations.lock.json, docs/CODEMAP.md, docs/explorations/SnackShot-IA-Overhaul-Guide.md | IA 개편 I3 — 투자 열람실(평가액·원칙 상시대조·종목 결정 마커), 유일한 신규 설계 principleWatch |
| 2026-07-14 | I1·I3 구현 검토(사후 검토): 무결성 독립 재검증(전 소스 NUL 스캔 0·신규 7파일 정상 종료·verify 재실행 PASS 25 versions) + 배선 전수 확인 — I1(getOutcomeHistory deleted 포함·타임라인 되돌림 라벨·진입점 4경로 push·미결 전이 store 재사용), I3(v25 additive·principleWatch 해시 캐시+조용 실패+H0·평가액 캐시우선/캡처 폴백/제외 캡션·마커 이분탐색/휴장 스냅/미래 미결 우측 점선·교훈 모음) 모두 정상. 결함 1건 수정: 결정 상세의 ✎ 수정이 미결에도 노출 — updateUserEdit(status='edited' 무조건)로 미결이 확정 오염(보드 이탈·마감 알림 취소) → 미결에서는 수정 숨김(onEdit optional, '결정했다' 전이가 편집 담당). verify PASS | app/decision/[id].tsx, src/components/decision/detail/DecisionDetailBody.tsx, docs/explorations/SnackShot-IA-Overhaul-Guide.md, CLAUDE.md | I 트랙 품질 게이트 — 미결 상태 불변식 보호 |
| 2026-07-14 | I2 구현(5탭 재편 + 설정 이동 · 스키마 무변경): 탭 Today·Archive·Inbox·**결정**·**투자**(설정은 탭에서 제거). (1) `DecisionBoard.tsx` 신설 — inbox의 보드 섹션(고민 중·후속 확인·진행 중·완료)·핸들러(회고·재후속·미결 전이·완료·롱프레스 액션)·모달을 추출, 스크롤 컨테이너는 부모 소유(중첩 방지). Inbox와 결정 탭이 useInboxStore 공유하며 재사용(신규 store 없음). (2) Inbox 축소 — 덱(+낮은확신)·후속 도래·**마감 도래 미결(decide_by≤now)**만. 진행중/완료/전체목록/통계·뷰토글 제거. 덱은 flex(제스처)라 due 항목은 상단 maxHeight 45% 스크롤+덱 flex 아래 배치. (3) `app/(tabs)/decisions.tsx` 신설 — DecisionStats(상시)+DecisionOnThisDay+DecisionBoard(미도래 미결·진행중·회고)+전체기록(DecisionList). DecisionList는 통계/스크롤/showInsights 제거하고 필터+카드 fragment로 축소(부모가 스크롤·통계 소유). old `app/decisions.tsx` 삭제(allow_cowork_file_delete로 FUSE unlink 허용)+root Stack.Screen name="decisions" 제거. (4) `app/invest.tsx`→`app/(tabs)/invest.tsx` 승격(back버튼→탭 타이틀 헤더+tabBar 하단 패딩). (5) 설정 `app/(tabs)/settings.tsx`→`app/settings.tsx`(스택) — useBottomTabBarHeight→useSafeAreaInsets, 상단 back버튼 추가. TodayHeader에 ⚙(onOpenSettings→/settings)+today.tsx 배선. (6) 배지 — `countDeliberatingDue` repo 신설, loadInbox·loadBadge 배지=후보+후속도래+마감도래 미결. (7) `Icon`에 decisions(checkbox)·invest(stats-chart) 추가. 딥링크 전수 확인: 후속·미결 알림 `/(tabs)/inbox` 유지(도래분이 inbox에 있어 정합), stale 라우트 없음. `npm run verify` PASS(25 versions), NUL 스캔 전 파일 클린. 탭 전환·설정 진입·보드 동작·배지 육안은 에뮬레이터 이월. **편차/주의**: ① 덱+due 항목 동거 레이아웃(덱 flex+due maxHeight)은 목업 미명시 — 제스처/스크롤 충돌 회피 위한 선택, 에뮬 확인 필요. ② DecisionList를 fragment로 축소(스크롤 부모 이관) — inbox 'list' 모드 제거로 유일 사용처가 결정 탭이라 안전. (부수: 세션 파일동기화가 file-tool Edit 5파일[Icon·decisions repo·inbox store·+I1의 detail 2파일은 사용자 수정본]에 truncation/NUL 재발 → git show HEAD:+python 재적용, 대형/신규 파일은 bash heredoc으로 기록해 회피) | app/(tabs)/{_layout,inbox,decisions,invest,today}.tsx, app/{settings,_layout}.tsx, src/components/decision/{DecisionBoard,DecisionList}.tsx, src/components/today/TodayHeader.tsx, src/components/ui/Icon.tsx, src/db/repos/decisions.ts, src/stores/inbox.ts, docs/CODEMAP.md, docs/explorations/SnackShot-IA-Overhaul-Guide.md | IA 개편 I2 — 5탭 정보구조(결정·투자 승격, Inbox 처리 전용화, 설정 스택 이동) |
| 2026-07-14 | I2 구현 검토(사후 검토): 무결성(NUL 0·라우트 구조 5탭/설정 스택/구 라우트 제거)·배지 3합(countDeliberatingDue)·stale 라우트 참조 0·TodayHeader ⚙·settings useSafeAreaInsets 전환·F4 재후속 hasMemo 게이트가 DecisionBoard로 이식되어 inbox(due 전용 보드 재사용)·결정 탭 양쪽 관통 — 전부 정상. Inbox의 due 항목을 DecisionBoard(upcoming/reflection 빈 배열) 재사용으로 처리한 설계는 처리 액션 SoT 유지로 타당. 결함 1건 수정: inbox obsidianPrompt가 Alert 선택 없이 openEntryInObsidian을 무조건 호출(편집 컨펌 저장 시 무단 앱 전환) → '닫기/옵시디언에서 열기' Alert 복원(DecisionBoard·상세와 동일 UX). 스와이프 컨펌의 안내 생략은 덱 리듬 우선으로 수용(export는 confirmDecision 내부에서 동작). verify PASS(25 versions) | app/(tabs)/inbox.tsx, docs/explorations/SnackShot-IA-Overhaul-Guide.md, CLAUDE.md | I2 품질 게이트 — 무단 앱 전환 차단 |
| 2026-07-14 | devSeed 주식 시드 추가(사용자 요청 — 에뮬레이터 투자 기능 테스트): 매매 결정 3건(005930 분할매수+good 회고·교훈·절반익절·AAPL 이벤트 대기 미결, structured_json 포함 — 실존 티커라 시세 키 있으면 실제 차트에 시드 마커가 얹힘), 포트폴리오 스냅샷 1건(holdings 각 항목 __seed__ 키 — HoldingSchema strip으로 화면 무영향·purge 식별, 카카오 고비중=원칙 충돌 소재), quotes 캐시 5행(오늘·어제 — 평가액·등락·시세대조 오프라인 동작). purge에 스냅샷(LIKE)·quotes(시드 티커 행, 캐시라 무해) 정리 추가. node:sqlite v1→v25 왕복 검증(매매 3건 조회·FTS 색인·purge 잔여 0), verify PASS. 릴리스 키 질의 응답: 설정 화면 시세·Twelve 키 입력은 H5/H6에서 기배선·I2 이동에서도 유지 확인(SecureStore 경로로 APK 동작, .env는 dev 전용) — 추가 구현 불필요 판정. (부수: router.d.ts 손상 재발 → 스텁 복구) | src/db/repos/devSeed.ts | 주식 기능(투자 탭·차트 마커·평가액·원칙 충돌)을 시드만으로 즉시 테스트 가능하게 |
