# 라인엔딩 정규화 — 저장소는 LF, 체크아웃은 플랫폼 기본.
# 2026-07-03: 워킹트리 전체 CRLF 변환으로 250개 파일 phantom-modified 발생 → eol 규칙 고정.
* text=auto

# bash가 CRLF를 못 읽으므로 셸 스크립트는 항상 LF
*.sh text eol=lf
gradlew text eol=lf

# Windows 배치 파일은 CRLF
*.bat text eol=crlf

# 바이너리
*.png binary
*.jpg binary
*.ttf binary
*.otf binary
